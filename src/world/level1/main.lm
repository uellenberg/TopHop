/*
This file defines the structure for the first level. Specifically, it contains the "main" world, not including any obstacles.
These types of objects exist on layer 0.
*/

usePrefab!(-1, "mount_point", 0, 0, 4, 18, 1);
usePrefab!(-1, "mount_point", 0, 0, -4, 18, 1);

usePrefab!(-1, "mount_point", 0, 15, 3, 9, 1);